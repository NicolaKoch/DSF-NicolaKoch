The kinase file containes the notebook that was executed locally

The ubelix file containes the notebook that was executed on ubelix

The rf file containes the notebook that was executed with an alternative model (RF)
